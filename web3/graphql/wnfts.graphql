query GetWNFTS($currentOwner: Bytes!) {
  wnfts(where: { currentOwner: $currentOwner }) {
    id
    currentOwner
    firstStakePrice
    lastPrice
    minHoldPeriod
    ogTokenAddress
    ogTokenId
    tokenId
  }
}
